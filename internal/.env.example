# Server-only proxy target for /api/* rewrites and RSC server-side fetches.
# NOT a NEXT_PUBLIC_* var — the browser stays same-origin and lets Next.js
# proxy to this URL.
API_PROXY_TARGET=http://localhost:6180
