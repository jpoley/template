{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information"
    }
  },
  "ConnectionStrings": {
    "_comment": "Leave Postgres empty to fall back to the in-memory store. Compose overrides this to point at the postgres service."
  }
}
